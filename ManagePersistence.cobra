use VariantMvc
namespace GenericProject

class ManagePersistence implements IRole

	get tasked_with as String
		return "optionally resuming on startup, the application's previous state (before closing or crashing)"
		
	def checkFeasability
		pass
